# Need to use this instead of aliasing `curl`
# See https://github.com/nvm-sh/nvm/issues/1150
#

# This always breaks install scripts etc because it makes in output invalid
# --include

--location
--silent
